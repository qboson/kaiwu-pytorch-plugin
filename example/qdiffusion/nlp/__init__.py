"""BM-only natural-language QDiffusion example.

The package initializer stays dependency-light so the CLI entrypoints can
bootstrap the repository-local ``kaiwu`` namespace before importing models.
"""
