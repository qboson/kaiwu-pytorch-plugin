# Install this repository first: pip install -e .
# Install a CUDA-matched PyTorch wheel separately when needed.
transformers>=5.0,<6
math-verify==0.9.0

# PyPI kaiwu is installed through this repository's project dependencies.
